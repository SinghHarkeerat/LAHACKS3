import React from 'react';
import Camera from './Camera';

const MutePage = () => {
    return (
        <div>
            <h1>This is the Mute Page</h1>
            <Camera />
        </div>
    );
};

export default MutePage;